  <div id="user<%= user.id %>" class="row">
    <div class="cell"><%= user.first_name %></div>
    <div class="cell"><%= user.last_name %></div>
    <div class="cell"><%= user.user_name %></div>
    <div class="cell_end action_links"><%= link_to "Delete", user, confirm: "Are you sure you want to delete this user?", remote: true, method: :delete %> </div>
  </div>
